export function buildProcurementSpec(analysis) {
  if (!analysis) return '';
  const standards = analysis.recommendations?.slice(0, 3).map((s) => s.standardNumber).join(', ') || 'Applicable standards to be verified';
  return `AI BIS SENSOR — PROCUREMENT SPECIFICATION (PROTOTYPE)\n\nProduct: ${analysis.product}\nCategory: ${analysis.category}\nApplication: ${analysis.application}\n\n1. Scope\nThe supplier shall provide the specified product for the stated procurement application and demonstrate conformity with the applicable requirements.\n\n2. Technical Requirements\n${analysis.specifications.map((x) => `- ${x}`).join('\n')}\n\n3. Safety Requirements\n${analysis.safety.map((x) => `- ${x}`).join('\n')}\n\n4. Testing & Evidence\n${analysis.testing.map((x) => `- ${x}`).join('\n')}\n\n5. Indicative Standards References\n${standards}\n\n6. Documentation\n- Manufacturer technical datasheet\n- Test reports/certificates where applicable\n- Installation and maintenance instructions\n- Warranty and traceability information\n\n7. Compliance Note\nThis specification is generated from local prototype/demo data. Verify current official BIS publications, amendments, scope and procurement rules before issuing a tender.`;
}
