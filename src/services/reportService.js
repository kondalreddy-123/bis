export function makeReport(a,checklist=[]){return `AI BIS SENSOR — PROCUREMENT INTELLIGENCE REPORT\nPrototype / Demo Data\n\nRequirement\n${a.text}\n\nProduct: ${a.product}\nCategory: ${a.category}\nApplication: ${a.application}\nConfidence: ${a.confidence}%\n\nExtracted Specifications\n- ${a.specifications.join('\n- ')}\n\nRecommended Standards\n${a.recommendations.map((s,i)=>`${i+1}. ${s.standardNumber} — ${s.title} (${s.score}% match)`).join('\n')}\n\nCompliance Gaps\n- ${a.missing.join('\n- ')}\n\nSafety Checklist\n${checklist.map(x=>`${x.done?'[x]':'[ ]'} ${x.text}`).join('\n')}\n\nVerify applicable requirements against official BIS publications before procurement.\nGenerated: ${new Date().toLocaleString()}`}
