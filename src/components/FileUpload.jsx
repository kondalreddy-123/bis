import {UploadCloud,FileText} from 'lucide-react'; import {useState} from 'react';
export default function FileUpload({onText}){const [file,setFile]=useState(null);const handle=async f=>{if(!f)return;setFile(f);if(f.type==='text/plain'){onText(await f.text())}else onText(`Demo uploaded document: ${f.name}. The browser-only prototype will use demo extraction for this ${f.type||'document'} file.`)};return <label className="upload"><input type="file" accept=".pdf,.docx,.txt" onChange={e=>handle(e.target.files?.[0])}/><UploadCloud size={30}/><b>{file?file.name:'Drop tender document here'}</b><span>PDF, DOCX, TXT · frontend demo mode</span><em><FileText size={14}/> Choose file</em></label>}
