import { PageTitle, Analyzer } from './shared';
export default function Analyze(props){return <><PageTitle eyebrow="WORKSPACE" title="Analyze Requirement" text="Use text, voice or a tender document to start a local prototype analysis." /><Analyzer {...props}/></>}
