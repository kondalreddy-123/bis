import { PageTitle, FileUpload } from './shared';
export default function TenderDocuments({setText,runAnalysis,setPage,showToast}){return <><PageTitle eyebrow="DOCUMENT INTELLIGENCE" title="Tender Documents" text="Upload procurement documents. TXT is read locally; PDF/DOCX use an explicit demo fallback." /><div className="panel"><FileUpload onText={x=>{setText(x);showToast('Document loaded')}}/><div className="actions"><button className="primary" onClick={()=>runAnalysis()}>Analyze Tender</button><button className="secondary" onClick={()=>setPage('Recommendations')}>Find Standards</button><button className="secondary" onClick={()=>setPage('Reports')}>Generate Report</button></div></div></>}
