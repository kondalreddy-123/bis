import { PageTitle, StandardTable } from './shared';
export default function Compare({analysis}){const selected=(analysis?.recommendations||[]).slice(0,4);return <><PageTitle eyebrow="DECISION SUPPORT" title="Compare Standards" text="Compare up to four candidate records side by side." />{selected.length?<><div className="panel"><StandardTable items={selected}/></div><div className="compare-grid">{selected.map(s=><div className="panel" key={s.id}><span className="badge blue">{s.standardNumber}</span><h3>{s.title}</h3>{[['Scope',s.scope?.join(', ')],['Application',s.applications?.join(', ')],['Safety',s.safetyRequirements?.join(', ')],['Testing',s.testingRequirements?.join(', ')],['Status',s.status],['Year',s.year]].map(([k,v])=><div className="compare-row" key={k}><span>{k}</span><b>{v||'—'}</b></div>)}</div>)}</div></>:<div className="empty"><h3>No standards to compare</h3><p>Run an analysis first.</p></div>}</>}
