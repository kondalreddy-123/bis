import { PageTitle, ComplianceBlock } from './shared';
import { FileDown } from 'lucide-react';
export default function Compliance({analysis,checks,setChecks,download,report}){return <><PageTitle eyebrow="READINESS" title="Compliance" text="Identify covered, missing and review-required procurement requirements." right={<button className="primary" onClick={()=>analysis&&download('compliance-report.txt',report)}><FileDown size={17}/> Generate Compliance Report</button>} /><ComplianceBlock analysis={analysis} checks={checks} setChecks={setChecks}/><div className="panel"><h2>Potentially outdated references</h2><p>Review any record marked <span className="badge amber">Review</span> before procurement. Prototype status is not an official compliance determination.</p></div></>}
